# forge-audio

> Audio → tiles → Plato agents.

Decomposes audio into chunks with spectral metadata: RMS energy, peak amplitude, zero-crossing rate, spectral centroid. Each chunk is a tile that Plato agents can read, filter, and transform.

Built from the SubForge pattern — audio was the first input type. Now it's a tile like everything else.

Apache 2.0
